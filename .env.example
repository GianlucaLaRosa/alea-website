# --- Vercel environments (Settings → Environment Variables) ---
# Assign each variable to Production, Preview, and/or Development.
# - Production: prod DB + secrets + NEXT_PUBLIC_SERVER_URL = https://your-domain.com
# - Preview: use a separate POSTGRES_URL (e.g. Neon branch / staging DB) if you must not migrate prod.
#   Set NEXT_PUBLIC_SERVER_URL to the preview URL (e.g. https://<deployment>.vercel.app) so links/previews work.
# - Development: values for `vercel dev` / local .env
#
# --- Sviluppo locale con Docker Desktop ---
# 1) Avvia Postgres: docker compose up -d
# 2) Copia questa riga (o adatta user/password/db in docker-compose.yml):
# POSTGRES_URL=postgresql://postgres:postgres@127.0.0.1:5432/payload
# 3) Prima migrazione: pnpm payload migrate
# Media: senza BLOB_READ_WRITE_TOKEN i file vanno in public/media/ (già in .gitignore).
#
# Database connection string (Vercel Postgres / Neon / Postgres locale)
POSTGRES_URL=postgresql://postgres:postgres@127.0.0.1:5432/payload
# Used to encrypt JWT tokens (use different values per environment)
PAYLOAD_SECRET=YOUR_SECRET_HERE
# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Vercel Blob — media uploads in cloud (Vercel → Storage → Blob). Opzionale in locale: lascia vuoto per usare public/media/
BLOB_READ_WRITE_TOKEN=
# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE
# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE